/** @format */

import { NEXT_THEME } from '@@lib/constants';
import { Theme } from '@mui/material';

export const baseLightTheme = {
    palette: {
        mode: NEXT_THEME.LIGHT,
    },
} as Theme;
